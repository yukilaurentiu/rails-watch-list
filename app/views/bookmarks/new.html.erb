<%= simple_form_for [@list, @movie, @bookmark] do |f| %>
  <%= f.input :name, collection: List.all %>
  <%= f.submit "Add list", class: "btn btn-success" %>
<% end %>

<%= link_to "Back", lists_path %>
