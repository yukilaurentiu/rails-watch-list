<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
   <%= f.association :movie %>
  <%= f.submit "Add Bookmark", class: "btn btn-success" %>
<% end %>

<%= link_to "Back", lists_path %>
